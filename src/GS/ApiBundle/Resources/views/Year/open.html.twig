{% extends "GSApiBundle::layout.html.twig" %}

{% block gs_body %}
    {{ parent() }}

    <h2>Ouvrir une année</h2>

    <p>
        Etes-vous certain de vouloir ouvrir l'année "{{ year.title }}" ?
    </p>

    <form action="{{ path('open_year', {'id': year.id}) }}" method="post">
        <a href="{{ path('view_year', {'id': year.id}) }}" class="btn btn-default">
            <i class="glyphicon glyphicon-chevron-left"></i>
            Retour a l'année
        </a>
        {# Ici j'ai écrit le bouton de soumission à la main #}
        <input type="submit" value="Ouvrir" class="btn btn-danger" />
        {# Ceci va générer le champ CSRF #}
        {{ form_rest(form) }}
    </form>

{% endblock %}
